from setuptools import setup

if __name__ == "__main__":
    # Configuration is primarily in pyproject.toml
    # This setup.py is for compatibility or for scenarios
    # where setuptools needs to be invoked directly.
    setup()